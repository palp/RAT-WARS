on:
  push:

jobs:
  test:
    name: Run GUT tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test
        uses: ceceppa/godot-gut-ci@main
        with:
          godot_version: 4.1.3
